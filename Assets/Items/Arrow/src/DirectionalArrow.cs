using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DirectionalArrow : MonoBehaviour
{
    /*
    [SerializeField] EventTrigger enableArrow;
    [SerializeField] EventTrigger disableArrow;
    [SerializeField] EventTrigger hideArrow;
    [SerializeField] EventTrigger showArrow;
    */
    /*
    MeshRenderer meshRenderer;
    
    // Start is called before the first frame update
    void Start()
    {
        meshRenderer = GetComponent<MeshRenderer>();
        //enableArrow.onEnterTrigger.AddListener(EnableArrow);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
    /*
    public void EnableArrow(bool val)
    {
        enabled = val;
    }

    public void HideArrow(bool val)
    {
        meshRenderer.enabled = !val;
    }
    */
}
